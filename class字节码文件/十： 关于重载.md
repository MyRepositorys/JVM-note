# 十： 关于重载

```java
重载的概念是：在一个类中出现两个或多个方法名相同，但是参数列表不同的方法，
在字节码层面就是，获取该方法的简单名称，若简单名称相同，则获取该方法的描述符，但是去掉描述符中的返回值类型，然后进行比对，描述符中的参数类型与顺序，
也就是说方法的返回值类型与重载无关，

在字节码文件中，方法的返回值类型，和参数列表是存放在一起，放在方法的描述符中的，也就是说，两个或多个方法的描述符不同的方法就可以在class字节码文件中共存，

总而言之，返回值类型不参与方法是否重载的判断中,
```

